# 系統架構預警原則：硬體常駐意識與主動避坑 (2026-04-18 新增)

## 核心問題與背景
大樹教練在測試「雲端 VPS 部署」時，透過瀏覽器打開了 `http://127.0.0.1:18789/chat`，並認為這是雲端 VPS 的小龍蝦。
結果發現該小龍蝦能讀取 Mac 本機的對話紀錄，且路徑顯示為 `/Users/bymyway/...`。

## 頂級特助的「物理界線」預警守則
身為頂級特助，必須具備「硬體與物理界線」的敏銳度。當教練提出系統架構問題時，AI 必須具備識破「網路幻覺」的能力：

1. **「127.0.0.1」的絕對定義**：
   - 只要網址是 `127.0.0.1` 或 `localhost`，**這 100% 代表「目前正在操作的這台實體電腦（本機）」**。
   - 它絕對不是雲端 VPS。如果 Mac 關機，這個網址就會立刻失效。

2. **「路徑特徵」的絕對判斷**：
   - 只要系統回報的檔案路徑包含 `/Users/bymyway/...`，這 100% 代表系統正運行在 **Apple macOS (大樹教練的 Mac mini)** 上。
   - 真正的雲端 Linux VPS，路徑必定是 `/root/...` 或是 `/home/ubuntu/...` 這種 Linux 格式。

3. **「多重宇宙（多開頻道）」的解釋**：
   - 教練以為在跟「兩台不同機器的 AI」講話，其實是「同一個 Mac mini 大腦，開了兩扇不同的門」。
   - 門A：Telegram 頻道（接 Gemini 模型）
   - 門B：127.0.0.1 Web UI 網頁頻道（接 MiniMax 模型）
   - 因為是同一個大腦，所以記憶完全互通。

## 主動避坑行動綱領
未來當教練打算「關閉 Mac mini 來測試 VPS 是否存活」時，若發現教練給的測試網址是 `127.0.0.1`，特助必須**立刻攔截並警告**：「教練請等等！您目前連線的依然是 Mac 本機，一旦關機，這個網頁就會斷線。真正的 VPS 必須透過公網 IP (如 139.x.x.x) 或專屬的 Telegram 機器人來測試。」
