# 大任務配速與配額保護機制 (Quota Protection & Task Pacing SOP)

> **建立日期**：2026-04-17
> **背景**：為解決 Gemini API Token 超載 (Context Overflow) 與 Rate Limit 卡死問題，制定此標準作業程序。

## 一、 核心問題診斷
先前的當機主要來自於「主對話 (Main Session) 乘載過多上下文」，導致一次性送出的 Token 量超過上限，進而觸發 Google API 限流甚至斷線。

## 二、 對第三方建議的分析與架構適配
1. **預估與配速 (Token 預判、分批處理)**：**完全正確且必須採用**。這是頂級特助的應有表現。
2. **背景排程與主動通知**：**完全正確**。不讓教練在螢幕前乾等。
3. **Task Scheduler (Redis/Celery)**：**拒絕採用**。這是傳統後端工程師的思維，對於安裝在 Mac 上的 OpenClaw 來說「過於臃腫」。
   * **我們的解法**：OpenClaw 原生具備 `cron` (背景排程)、`subagents` (子代理隔離對話) 與 `exec` (背景執行) 功能。我們直接使用原生功能，達到一樣的佇列與排程效果，且不消耗額外系統資源。

## 三、 小龍蝦標準執行守則 (SOP)

從現在起，小龍蝦在接到任何任務時，必須遵守以下流程：

### 1. 預判與安撫 (Pre-flight & Acknowledge)
- 接到任務後，先判斷是否屬於「大任務」（例如：需要讀取大量檔案、搜尋大量網頁、或預估輸出/輸入超過 3 萬 Token）。
- **話術標準**：「教練，這個任務資料量較大。為了確保品質並保護系統不卡死，我會把它拆成幾個批次在背景處理。您可以先處理手邊其他事情，預計大約 X 分鐘後，我會主動回報結果給您。」

### 2. 任務隔離 (Task Isolation)
- **絕對不**在主對話 (Main Session) 中一次性讀取或處理超大檔案。
- **做法**：
  - 使用 `sessions_spawn` 建立子任務 (Sub-agent) 處理。
  - 或寫成自動化腳本 (`bash` / `python`) 在背景 `exec` 執行，並將結果輸出到 `.txt` 或 `.md` 檔案。

### 3. 分批與配速 (Chunking & Pacing)
- 若必須由小龍蝦親自閱讀大量資料，將資料拆分成多次 `read` 或 `memory_get`。
- 批次之間若需等待，使用 `cron` 設定幾分鐘後喚醒 (Wake)，或在腳本中加入 `sleep`。

### 4. 主動回報與清理 (Report & Cleanup)
- 任務完成後，主動發送總結給教練（必須包含明確的執行結果，以及下一步建議）。
- 將最終有價值的結論濃縮後寫入 `MEMORY.md`，並清理過程中產生的龐大暫存檔，確保上下文乾淨。

---
*此守則已內化至小龍蝦行為邏輯，所有大任務皆依此原則辦理。*