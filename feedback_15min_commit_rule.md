---
name: 每 15 分鐘上傳一次工作記錄（最高指導原則）
description: 任何長任務都必須每 15 分鐘 commit + push 一次；給其他終端機/助教的指令也必須帶上這條
type: feedback
originSessionId: 0d047e5b-f32f-438b-bf9b-ee1440e905b1
---
## 規則
不管在做什麼任務，每 15 分鐘就要把工作記錄 commit 起來、上傳一次。最壞情況下，當機或發生意外只損失最近 15 分鐘未上傳的部分。

**Why:** 教練多次強調這是「最高指導原則」。AI 助教容易在長任務中跑了一兩小時，結果一個意外就把整片成果全沒了。15 分鐘 = 可接受的最大損失上限。

**How to apply:**
1. **自己跑長任務時** —— 每完成一個檔案、一個段落、一個子任務，就 commit + push 並回報 hash
2. **寫指令給其他終端機/AI 助教時** —— 在指令最後**必須**加上「每 15 分鐘上傳一次」這條，不能漏
3. 範例段落（直接複製進指令尾巴）：
   ```
   ⚠️ 最高指導原則：每 15 分鐘就要把當下進度上傳 commit 一次，不要等到全部做完才存。
   萬一當機或斷線，最多只損失 15 分鐘的進度。請不斷提醒自己這條原則。
   ```
4. 給的指令如果用到 git，明確寫出「每 15 分鐘 git add + git commit -m + git push」
5. 沒 git repo 的任務，可以改成「每 15 分鐘把當下產出寫到輸出檔，append 不要覆蓋」

## 觸發暗號
- 「你忘了 15 分鐘」「commit 一下」「上傳一下」→ 立刻 commit 推一次當前進度
