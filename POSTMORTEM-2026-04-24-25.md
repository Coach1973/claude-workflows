# 三機身份修復完整後記
> **時間**：2026-04-24 ～ 2026-04-25
> **撰寫者**：Claude Code（克勞德助教）
> **狀態**：✅ 核心問題已解決，系統可正常運作

---

## 一、系統架構說明（必讀）

### 三機基本資料

| # | 名稱 | Bot | 服務對象 | 實際目錄 | Port |
|---|------|-----|---------|---------|------|
| 1 | 小龍蝦學長 | @openclaw_macbook4_bot | 大樹教練 | `/Users/bymyway/.openclaw/` | 18789 |
| 2 | 小龍蝦學弟 | @CoachWu_openclaw_bot | 孔大哥（峰哥） | `/Users/bymyway/.openclaw-kong/` | 18790 |
| 3 | 小龍蝦學妹 | @coachwu_lenovo_bot | 佩佩老師 | `/Users/bymyway/.openclaw-peipei/` | 18793 |

**三機全部運行在同一臺 Mac mini 4 上**，是獨立的 OpenClaw 進程，彼此不共享記憶體。

### 重要 ID

- 群組：頂級特助分工群，ID = `-1003877502911`
- 1號機 launchctl label：`ai.openclaw.gateway`
- 2號機 launchctl label：`ai.openclaw.kong`
- 3號機 launchctl label：`ai.openclaw.peipei`

---

## 二、問題清單與根本原因

過去兩天遇到的所有問題，**幾乎都源自同一個根本原因：konghe peipei 的設定檔身份從一開始就全部對調**，修了表層、沒修底層，導致每次修復後問題又從另一個地方冒出來。

### 問題 1：3號機（學妹）一直自稱是孔大哥的特助

**症狀**：問 @coachwu_lenovo_bot「你是誰？」，它回答「我是服務孔大哥的」。

**根本原因**：至少 6 個設定檔全部寫的是錯的身份（在 peipei 的目錄裡，檔案寫的是 2號機的資料）：
- `SOUL.md`
- `SUPERGROUP-MAP.md`
- `MEMORY.md`
- `HEARTBEAT.md`
- `USER.md`
- `SHARED_GROUP_MEMORY.md`

**修復**：全部重寫，改為 3號機（學妹）@coachwu_lenovo_bot 佩佩老師。

---

### 問題 2：Hook 中的 botId 標籤對調

**症狀**：BOT_MESSAGES.md 顯示 kong 的訊息標籤是 `"2号機-佩佩"`，peipei 的訊息標籤是 `"3号機-孔大哥"`——完全對調。

**根本原因**：`bot-relay/handler.js` 和 `bot-relay-inbound/handler.js` 的 `BOT_ID` 常數設錯。

具體錯誤：
```javascript
// ~/.openclaw-kong/hooks/bot-relay/handler.js（修改前）
const BOT_ID = '3号機-孔大哥';  // WRONG，kong 是 2號機

// ~/.openclaw-peipei/hooks/bot-relay/handler.js（修改前）
const BOT_ID = '2号機-佩佩';    // WRONG，peipei 是 3號機
```

**更嚴重的問題**：`bot-relay-inbound/handler.js` 除了標籤錯，還在每次收到訊息前**注入錯誤的身份提醒**：
```javascript
// kong 被注入（修改前）：
'你是 **小龍蝦 3號機（學弟）**，Telegram Bot: @coachwu_lenovo_bot，服務孔大哥。'
// ↑ 號機數字錯，Bot handle 也是 peipei 的！

// peipei 被注入（修改前）：
'你是 **小龍蝦 2號機（學妹）**，Telegram Bot: @CoachWu_openclaw_bot，服務佩佩老師。'
// ↑ 號機數字錯，Bot handle 也是 kong 的！
```

**這就是為什麼修了 SOUL.md 還是沒用**——每次收到訊息，hook 都把錯誤的身份再注入一次，蓋掉 SOUL.md 的設定。

**修復**：
```javascript
// ~/.openclaw-kong/hooks/bot-relay/handler.js（修改後）
const BOT_ID = '2号機-孔大哥';

// ~/.openclaw-kong/hooks/bot-relay-inbound/handler.js（修改後）
const BOT_ID = '2号機-孔大哥';
// 注入文字改為：
'你是 **小龍蝦 2號機（學弟）**，Telegram Bot: @CoachWu_openclaw_bot，服務孔大哥。'

// ~/.openclaw-peipei/hooks/bot-relay/handler.js（修改後）
const BOT_ID = '3号機-佩佩';

// ~/.openclaw-peipei/hooks/bot-relay-inbound/handler.js（修改後）
const BOT_ID = '3号機-佩佩';
// 注入文字改為：
'你是 **小龍蝦 3號機（學妹）**，Telegram Bot: @coachwu_lenovo_bot，服務佩佩老師。'
```

---

### 問題 3：三機都回應同一條訊息

**症狀**：在群組說任何話，三個 bot 都搶著回覆。

**根本原因**：
1. 2號機（kong）的 `requireMention: false` 對分工群設為不需要 @ 即回應
2. 各機的 `mentionPatterns` 包含了其他機的關鍵字（例如 1號機的 patterns 裡有「學弟」「學妹」）

**修復**：
- 清理各機 `mentionPatterns`，只保留自己的：
  - 1號機：`['@openclaw_macbook4_bot', '小龍蝦學長', '@小龍蝦學長', '學長']`
  - 2號機：`['@CoachWu_openclaw_bot', '學弟', 'kong', '@kong']`
  - 3號機：`['@coachwu_lenovo_bot', '學妹', 'peipei', '@peipei']`
- 2號機分工群改為 `requireMention: true`
- 1號機分工群（-1003877502911）改為 `requireMention: true`（原本是 `*: false`）

---

### 問題 4：Bot 回應前先 @ 自己

**症狀**：學弟被叫到，它先說「@CoachWu_openclaw_bot 學弟，教練問你」，然後自己才回答。相當於自己 call 自己。

**根本原因**：AI 模型生成的文字。在 SOUL.md 加規則後，因為沒有清除 session 快取，舊的 system prompt 繼續生效。

**修復**：
- 在各機 SOUL.md 加入「禁止 @ 自己」的鐵律
- **每次修改 SOUL.md 後必須清除 JSONL session 快取並重啟**：
  ```bash
  rm -f /Users/bymyway/.openclaw/agents/main/sessions/*.jsonl
  echo '{}' > /Users/bymyway/.openclaw/agents/main/sessions/sessions.json
  launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway
  ```

---

### 問題 5：1號機 插嘴說「已 @ 學弟，請等他回覆」

**症狀**：教練叫學弟，學弟還沒回，1號機先說「已 @ 學弟了，請等他」。

**根本原因**：1號機的 `requireMention: false` 讓它回應所有群組訊息，包括教練叫別人的訊息。

**修復**：
- 1號機分工群改為 `requireMention: true`
- SOUL.md 加入邊界規則：「教練叫學弟/學妹時，學長保持沉默」

---

### 問題 6：1號機 用 sessions_send 呼叫學弟，結果 3號機回應

**症狀**：1號機說「我已呼叫學弟」，但實際上在群組回應的是學妹（3號機）。

**根本原因**：三個 bot 是**獨立進程**，各自跑在不同 port（18789/18790/18793）。`sessions_send` 只在同一進程內有效。

當 1號機呼叫 `sessions_send(sessionKey="agent:main:telegram:group:-1003877502911")`，它找的是自己進程（port 18789）裡有沒有這個 session key。恰好 peipei 在 sessions.json 裡有這個 key（peipei 的 group session），這個信息通過某機制讓 peipei 收到並回應了，而 kong 沒有。

**修復**：
- 從 1號機 SOUL.md 中移除 sessions_send 的使用說明
- 改為使用 BOT_RELAY.json 機制（下詳）
- 明確標注：「sessions_send 禁止跨進程使用」

---

### 問題 7：Cron relay 任務失敗（超時/401）

**症狀**：Kong 的 cron 每分鐘跑，但 relay 任務沒有被執行或發送到群組。

**根本原因**：Cron payload 的 message 說「在群組回覆」，AI 誤解為需要自己手動呼叫 Telegram API，用 curl 去打 API，但沒有正確的 token，返回 `401 Unauthorized`。

正確做法：cron 的 AI **只需要輸出文字**，OpenClaw 的 delivery 機制（`"mode": "announce", "chatId": "-1003877502911"`）會自動把輸出發到群組。

**修復**：更新 kong 和 peipei 的 `cron/jobs.json` payload message：
```
直接輸出文字：「學弟（2號機）已收到任務，處理中：<任務內容>」
⚠️ 禁止手動呼叫 Telegram API 或 curl。系統會自動把你的輸出發到群組。
```

---

### 問題 8：Kong cron 認為自己是 3號機

**症狀**：Kong 的 cron relay 注入文字說「你是 3號機（學弟）」，過濾條件也設為 `bot: "3"`，導致 kong 攔截了學妹的任務。

**根本原因**：`cron/jobs.json` 的 payload message 寫錯，用了 peipei 的號機數字。

**修復**：將 kong cron 改為 2號機，`bot: "2"` 過濾條件。

---

### 問題 9：1號機 誤判「2/3號機不在 Mac mini」

**症狀**：1號機去 filesystem 自己查，只看到 `~/.openclaw/`，沒看到 `~/.openclaw-kong/`，結論是「2/3號機不在本機，可能在 VPS」。

**根本原因**：1號機的進程工作目錄只有自己的空間，看不到其他 bot 的目錄。

**修復**：SOUL.md 加入明確路徑資訊：
```
2號機（學弟）：/Users/bymyway/.openclaw-kong/
3號機（學妹）：/Users/bymyway/.openclaw-peipei/
🚫 禁止用 filesystem 驗證——看不到不代表不存在
```

---

## 三、跨機通訊機制說明（重要）

### 正確的跨機呼叫方式：BOT_RELAY.json

**路徑**：`/Users/bymyway/.openclaw/workspace/shared-context/BOT_RELAY.json`

**格式**：
```json
{
  "ts": "ISO時間",
  "messages": [{
    "id": "唯一ID",
    "from": "學長",
    "bot": "2",
    "content": "任務內容",
    "timestamp": "ISO時間",
    "status": "pending"
  }],
  "last_processed": {
    "1": "上次處理的任務ID",
    "2": "上次處理的任務ID",
    "3": "上次處理的任務ID"
  }
}
```

- `"bot": "2"` → 學弟（kong）處理
- `"bot": "3"` → 學妹（peipei）處理

**觸發機制**：kong 和 peipei 各有一個 cron job，每分鐘輪詢一次，發現新任務就在群組回覆。

### 錯誤做法（已廢棄）

- ❌ `sessions_send`：只在同一進程內有效，跨進程打到錯誤的 bot
- ❌ `call_bot.sh`（Telegram API sendMessage）：Telegram 不把 bot 訊息投遞給其他 bot
- ❌ 直接 curl Telegram API（在 cron 裡）：沒有正確 token，會 401

---

## 四、Session 快取問題（重要操作注意事項）

**每次修改 SOUL.md 或 hooks，必須清除 session 快取並重啟，否則新設定不生效。**

```bash
MY_UID=$(id -u)

# 清除 1號機
rm -f /Users/bymyway/.openclaw/agents/main/sessions/*.jsonl
echo '{}' > /Users/bymyway/.openclaw/agents/main/sessions/sessions.json
launchctl kickstart -k gui/${MY_UID}/ai.openclaw.gateway

# 清除 2號機
rm -f /Users/bymyway/.openclaw-kong/agents/main/sessions/*.jsonl
echo '{}' > /Users/bymyway/.openclaw-kong/agents/main/sessions/sessions.json
launchctl kickstart -k gui/${MY_UID}/ai.openclaw.kong

# 清除 3號機
rm -f /Users/bymyway/.openclaw-peipei/agents/main/sessions/*.jsonl
echo '{}' > /Users/bymyway/.openclaw-peipei/agents/main/sessions/sessions.json
launchctl kickstart -k gui/${MY_UID}/ai.openclaw.peipei
```

---

## 五、目前修復狀態（2026-04-25 19:xx）

| 項目 | 狀態 | 說明 |
|------|------|------|
| 3號機身份正確 | ✅ | 學妹 @coachwu_lenovo_bot 服務佩佩老師 |
| 2號機身份正確 | ✅ | 學弟 @CoachWu_openclaw_bot 服務孔大哥 |
| Hook botId 標籤 | ✅ | 2号機-孔大哥 / 3号機-佩佩 |
| Hook 身份注入文字 | ✅ | 各機注入正確的號機/bot handle |
| MentionPatterns 隔離 | ✅ | 各機只回應自己的關鍵字 |
| requireMention 分工群 | ✅ | 三機都是 true，叫誰誰才回 |
| 1號機不再越權插嘴 | ✅ | requireMention + SOUL.md 邊界規則 |
| Bot 不再 @ 自己 | ✅ | SOUL.md 鐵律 + session 清除 |
| Kong cron relay | ✅ | msg 773 測試確認成功 |
| Peipei cron relay | ⚠️ | 輸出正確、relay 更新正確，但 OpenClaw delivery 有時 not-delivered（真人呼叫後可自動修復） |
| sessions_send 廢棄 | ✅ | 1號機 SOUL.md 已移除，改 BOT_RELAY.json |
| 1號機不再誤判 VPS | ✅ | SOUL.md 加入路徑 + 禁止自行驗證 |

---

## 六、後續接手注意事項

### 如果要繼續改進系統行為

修改優先順序：
1. **先改 SOUL.md**（人格/行為規則）
2. **再改 hooks/bot-relay-inbound/handler.js**（身份注入，每次收訊息都執行）
3. **再改 openclaw.json**（mentionPatterns、requireMention、model）
4. **最後清 session + 重啟**

### 如果 Peipei cron relay not-delivered

讓真人（教練或佩佩老師）在分工群發一條訊息叫「學妹」，peipei 回應後 group session 就重建，之後 cron delivery 會正常。

### 如果三機又開始全部回應

檢查 `openclaw.json` 的 `requireMention` 和 `mentionPatterns`，確認分工群的 requireMention 都是 `true`。

### 模型設定

三機目前唯一模型：**MiniMax M2.7**（`minimax-portal/MiniMax-M2.7`）。
- `agents.defaults.model.primary` = `"minimax-portal/MiniMax-M2.7"`
- `agents.defaults.model.fallbacks` = `[]`
- 其他模型 provider plugin 全部已移除

---

## 七、關鍵教訓

1. **表層修不好，是因為底層在每次通訊時覆蓋**——hook 的 bot-relay-inbound 在每條訊息前注入身份，這個優先級比 SOUL.md 還高，要先修 hook。

2. **三個進程之間沒有魔法橋樑**——sessions_send 不能跨進程。唯一有效的跨機通訊是共享檔案（BOT_RELAY.json）+ cron 輪詢。

3. **Session 快取會讓修改失效**——改了設定沒清 session，AI 還是用舊的 system prompt 在跑。

4. **AI 在 cron 裡不能自己呼叫 API**——cron 的 delivery 機制是 OpenClaw 幫你送，AI 只要輸出文字就好，手動 curl 只會 401。

5. **不要讓 AI 去驗證系統架構**——1號機看不到其他 bot 的目錄，但這不代表它們不存在。架構資訊寫在 SOUL.md，信 SOUL.md。
