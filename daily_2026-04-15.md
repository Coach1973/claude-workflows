# 2026-04-15 工作日誌

## 今日完成事項

### 1. Mac 記憶同步
- 從 GitHub clone `Coach1973/mac-openclaw-workflows` 到 `~/Documents/mac-openclaw-workflows`
- 複製 42 個 .md 記憶檔到 `/Users/bymyway/.openclaw/workspace/`
- 更新 HEARTBEAT.md 加入 Mac 版本標頭

### 2. Acer 宏碁當機修復（Kimi K2.5 上下文爆炸問題）
**根本原因**：AGENTS.md 要求啟動時一次載入 SOUL.md + USER.md + MEMORY.md + daily log，K2.5 上下文塞不下導致當機。

**解決方案**：製作 `AGENTS_KIMI_LITE.md` 輕量版：
- 啟動只讀 HEARTBEAT.md（最小必要）
- 其餘記憶按需讀取，不一次全載入
- 製作 `acer-fix-install.bat` 一鍵安裝腳本

**教訓**：K2.5 等上下文有限的模型，必須用輕量版 AGENTS.md，不能套用標準版。

### 3. Mac 小龍蝦 requireMention 修復
**問題**：Mac 機器人在群組需要被 tag 才回應，Acer 不需要。

**根本原因**：`openclaw.json` 第 119 行 `"requireMention": true`

**修復**：改為 `"requireMention": false`，重啟 Gateway 生效。

### 4. 雙機器人討論協議
在 HEARTBEAT.md 加入雙機討論狀態機：
- 教練說話 → 觸發討論（STATUS: DISCUSSING）
- 40 分鐘到 → 強制產出 1~3 條結論（STATUS: CONCLUDING）
- 結論後進入 2 小時冷卻（STATUS: COOLDOWN）
- 防無限循環：不回應對方的結論訊息和 HEARTBEAT_OK
- ROUND 超過 20 輪強制收尾

### 5. Open WebUI 安裝
**安裝方式**：pip install（Python 3.11，非 Docker）

**踩坑記錄**：
- Python 3.9 太舊，需升級到 3.11
- Anthropic API 不是標準 OpenAI 格式，Base URL 要用代理商的
- 教練的 API Key 是 easyclaude.com 的代理服務，Base URL 是 `https://api.easyclaude.com/v1`，不是 `https://api.anthropic.com/v1`
- Open WebUI 的 `OPENAI_API_CONFIGS` 欄位在此版本無效，需在網頁介面手動設定 Connections

**開機自動啟動**：已設定 launchd plist（`~/Library/LaunchAgents/com.openclaw.openwebui.plist`）

**三台共用網址**：`http://192.168.0.61:3000`（同一 WiFi 下）

### 6. Opcode 安裝
- 下載：`https://github.com/winfunc/opcode/releases/download/v0.2.0/opcode_v0.2.0_macos_universal.dmg`
- 安裝到 `/Applications/opcode.app`
- 自動讀取系統環境變數的 API Key，不需要手動填寫
- 工作區選 `/Users/bymyway`（根目錄，權限最大）

### 7. 聯想 Windows 電腦安裝 Claude Code
**步驟**：
1. 安裝 Node.js LTS（v24.14.1）
2. PowerShell 執行政策：`Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`
3. `npm install -g @anthropic-ai/claude-code`
4. 執行 `claude` 啟動

**結果**：自動讀取 GitHub 記憶，語音糾偏正常，三台記憶完全同步。

### 8. 小龍蝦模型精簡
**調整前**：Primary = gemini-2.5-flash，5 個 fallback 模型

**調整後**：Primary = `google/gemini-3.1-pro-preview`，fallbacks 清空

**原因**：教練已啟用 Google 帳單服務，流量充足，不需要自動切換。備援模型（Kimi、MiniMax）的 API Key 仍保留在設定檔，需要時隨時可加回。

---

## 踩坑總結

| 坑 | 原因 | 解法 |
|----|------|------|
| Acer 小龍蝦當機 | K2.5 上下文不足，一次載入太多記憶 | 輕量版 AGENTS.md，按需讀取 |
| Open WebUI 無模型 | Base URL 填錯（用了 Anthropic 官方 URL） | 改用 easyclaude.com 代理 URL |
| Windows npm 無法執行 | PowerShell 執行政策限制 | Set-ExecutionPolicy RemoteSigned |
| API Key 無效 | easyclaude.com 是代理服務，不是官方 Anthropic Key | Base URL 必須用代理商提供的網址 |

---

## 保留的 API Keys（備用）

- Kimi (Moonshot)：存於 openclaw.json models.providers.moonshot
- MiniMax：OAuth 模式，存於 openclaw.json
- Groq / Cerebras / SambaNova / OpenRouter：存於 openclaw.json env

---

## 系統現況（2026-04-15 結束時）

- Mac：Claude Code + Open WebUI（port 3000）+ Opcode + 小龍蝦 Gateway
- Acer：小龍蝦系統（輕量版 AGENTS.md，Kimi K2.5）
- 聯想：Claude Code（記憶同步完整）
- 雙機討論協議：已寫入 HEARTBEAT.md，待實測
- 小龍蝦主模型：Gemini 3.1 Pro（單一模型，無備援）
