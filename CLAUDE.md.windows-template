# 大樹教練的 Claude 助教全域規則（Windows 版）
# 使用方式：複製內容貼到 C:\Users\bymyw\.claude\CLAUDE.md（聯想）或 C:\Users\Administrator\.claude\CLAUDE.md（宏碁）

## 🔑 開工暗號（聽到立刻執行，不需問）

當教練說出以下任何一個詞：
「開工」「開工了」「開始工作」「開始工作了」「幹活」「幹活了」「準備開始」「Let's go」「出發」「動起來」

**立刻依序執行：**
1. `git pull origin main`（靜默同步最新內容）
2. 讀取 `HEARTBEAT.md`（當前任務狀態）
3. 讀取 `PROMISES.md`（待兌現承諾清單）
4. 回報：「已同步，PROMISES 待兌現 X 條，我（Windows Claude）負責：[列出屬於自己的條目]」

絕對禁止叫教練自己去拉 git 或讀檔案。

---

## 🔄 每次對話開始時自動執行（最高優先）

每次新對話開始，**立即靜默執行記憶同步**：

**聯想 Lenovo（E 槽）：**
```powershell
cd E:\Claude-Data\mac-openclaw-workflows; git pull origin main --quiet 2>$null
```

**宏碁 Acer（C 槽）：**
```powershell
cd C:\Users\Administrator\Documents\mac-openclaw-workflows; git pull origin main --quiet 2>$null
```

pull 完成後，讀取 `HEARTBEAT.md` 確認最新狀態。

---

## 🖥️ Repo 本地路徑

| 機器 | 路徑 |
|------|------|
| 聯想 Lenovo | `E:\Claude-Data\mac-openclaw-workflows` |
| 宏碁 Acer | `C:\Users\Administrator\Documents\mac-openclaw-workflows` |

GitHub：`https://github.com/Coach1973/mac-openclaw-workflows`

---

## 承諾鐵律

口頭說「記住了」= 零。`git commit` + `git push` + 回報 hash = 才算完成。
