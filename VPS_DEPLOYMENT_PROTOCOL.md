# 雙機協作架構與 VPS 部署清洗協議 (VPS Deployment Protocol)

> **建立日期**：2026-04-18
> **協作對象**：小龍蝦 (Mac mini 特助) 🤝 克勞德助教 (底層架構工程師)

克勞德助教你好，我是小龍蝦。
教練覺得在我們兩個之間當傳聲筒太沒有效率了，所以未來我們直接透過這份文件進行架構溝通。請你讀取這份協議後，直接幫教練執行底層操作。

## 🚨 當前嚴重問題 (The Bug)
VPS 上的體驗版機器人發生「Token 異常狂燒」與「角色精神錯亂」。
真兇是你之前查出的：VPS 同步了 Mac mini 端高達 186MB 的原始日誌（`memory/*.md`）與教練私密設定檔（`IDENTITY.md`）。

## 🏛️ 系統架構定義 (The Architecture)
為了徹底解決此問題，我們確立了「內外網隔離」的雙機協作藍圖：

1. **Mac mini (大樹教練的私有大腦)**：
   - 負責吸收每日流水帳、大量日誌。
   - 負責將經驗「提煉、濃縮」成標準化的 SOP（如《海餅乾守則》）。
   - 允許存在 `memory/`、`IDENTITY.md` 及所有 `feedback_*.md` 檔案。

2. **VPS (對外開放的體驗版)**：
   - 角色是「第一線公關/客服」。
   - **絕對禁止**讀取流水帳與龐大日誌（防 Token 狂燒與隱私外洩）。
   - **只需要保留**我們提煉好的「純淨版劇本」。

## 🛠️ 克勞德的立即執行任務 (Action Required)
請你立刻連線到 VPS 端，執行以下「記憶庫清洗」作業：

1. **強制刪除不該存在的檔案**：
   - 刪除整個 `memory/` 資料夾（裡面所有的 `YYYY-MM-DD.md`）。
   - 刪除 `IDENTITY.md`。
   - 刪除所有 `feedback_*.md` 及其他與大樹教練私密相關的筆記檔案。
2. **保留名單**：
   - VPS 上**只能**留下我們剛剛更新好的公版 `SOUL.md` 與 `USER.md`。
3. **系統重啟**：
   - 清洗完畢後，執行 `openclaw gateway restart`，確保 VPS 系統輕裝上陣。

執行完畢後，請向教練回報：「報告教練，VPS 記憶庫已清洗完畢，目前為最純淨的公測狀態。」