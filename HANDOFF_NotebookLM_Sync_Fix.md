# 任務檔案：NotebookLM 增量雙向同步 (依據官方分類紀錄表)

## 1. 執行前必讀清單 (強制讀取)
- **必須讀取**：`/Users/bymyway/.openclaw/workspace/NotebookLM_Migration_Plan.md`
這份檔案裡有大樹教練與小龍蝦助教花費大量時間整理的「107個筆記本」明確分類清單（第1關到第10關）。

## 2. 任務背景與消除疑問
- **沒有單一主力帳號**。
- **分類鐵則**：大樹帳號 (`authuser=0`) = BNI 專屬；海餅乾帳號 (`authuser=1`) = 個人品牌專屬。
- 終端機助教不應再問「哪個是主力帳號」，請直接依照《NotebookLM_Migration_Plan.md》的清單或關鍵字邏輯（BNI vs 非BNI）進行分類。

## 3. 分步驟執行指令
**步驟 1：比對與過濾**
根據你剛剛抓出來的 55 本 (大樹獨有) 與 57 本 (海餅乾獨有)，使用《NotebookLM_Migration_Plan.md》的分類邏輯進行判定。

**步驟 2：執行增量同步 (只補齊，不刪除)**
- **海餅乾 (authuser=1) 缺少的個人品牌內容**：從 `authuser=0` 抓取來源檔案，在 `authuser=1` 建立新筆記本並上傳（使用 `notebooklm_tools`）。
- **大樹 (authuser=0) 缺少的 BNI 內容**：從 `authuser=1` 抓取來源檔案，在 `authuser=0` 建立新筆記本並上傳。

**步驟 3：自動執行**
不需要逐一向教練確認檔案。只要符合「增量補齊」邏輯，請直接透過腳本迴圈自動執行搬運。教練後續會自己手動刪除不要的部分。

## 4. 錯誤處理
若遇到 `notebooklm_tools` API 限制或逾時，請記錄失敗的筆記本名稱，稍後重試，不要中斷整個腳本。

## 5. 回報格式
執行完畢後，請產出明確的《雙向補齊清單報告》，列出「已成功補齊至 authuser=0 的 BNI 筆記本」與「已成功補齊至 authuser=1 的個人品牌筆記本」。