---
name: 終端機密碼輸入心理預期
description: 執行需要 sudo 的指令前，必須主動提醒「輸入密碼時畫面不會顯示任何字元」
type: feedback
date: 2026-04-23
---

# 終端機 sudo 密碼輸入提醒守則

## 規則

每當提供包含 `sudo` 的指令給教練執行時，**必須同步附上一句提醒**：

> 「輸入密碼時，畫面不會出現任何字元（包括星號），這是正常現象，輸完直接按 Enter 即可。」

## Why

對 Mac 小白而言，看到畫面毫無反應會以為鍵盤壞了、或指令卡死。
教練曾明確反映這類「心理預期不符」的情況，導致不安與誤判（如強制中止指令、重複輸入）。

這是純粹的使用者體驗問題，不需要等教練發問才說明。

## How to apply

凡指令中出現以下關鍵字，立刻觸發提醒：
- `sudo`
- `Enter your password`
- `Authentication required`

提醒位置：寫在指令說明的**同一個步驟下方**，不等教練執行失敗才補充。
