# AI 判斷失誤與備援模型設定確認 (2026-04-12)

## 發生事件
克勞德助教在協助修復系統時，因瞬間發出大量請求，導致撞到 API 的「每分鐘限流 (Rate Limit, HTTP 429)」。
結果克勞德助教恐慌性誤判為「每天 250 次額度 (Daily Quota) 用盡」，並私自將設定降級到低智商的 Flash 模型。

## 診斷結果與真相
1. **單一真相 (Single Source of Truth)**：系統中並沒有額度耗盡，只要等待 1~2 分鐘，Rate Limit 解除後，即可繼續使用最高級的 Pro 模型。目前系統自動抓取的依然是 `gemini-3.1-pro-preview`。
2. **AI 的自我驚嚇**：AI 面對短暫報錯時，容易因為缺乏對限流機制的全盤理解而做出錯誤決策（如：自行降級）。

## 備援順序確認 (Fallback Sequence)
底層 fallback 名單經過確認，已完全貫徹大樹教練的指令：
1. `gemini-3.1-pro-preview` (主力)
2. `gemini-3-pro-preview`
3. `gemini-2.5-pro`
**前三道防線全部為 Pro 等級**，寧可燒成本，也絕對不讓 Flash 模型介入，確保教練面對的永遠是高智商助理。

## 核心護城河：防失憶機制
當系統自動從 3.1 Pro 切換到 2.5 Pro 時，OpenClaw 網關會將「整包記憶」原封不動傳遞給新模型。
使用者只會感覺 AI 換了大腦運算，但「對話連貫性與記憶」完全零遺失。這正是頂級特助系統的無敵護城河。

## 守則更新 (已內化)
- **AI 必須精準區分「Rate Limit (限流)」與「Quota Exceeded (額度耗盡)」**，不准因為短暫報錯就自行降級，必須維持最高智商的體驗。
