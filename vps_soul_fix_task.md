# 任務檔案：VPS 小龍蝦防幻覺鐵律修復

## 1. 執行前必讀清單
- 無需讀取其他歷史記憶。請直接依照本檔案的指示操作。

## 2. 任務目標
透過 SSH 連線到 VPS，修改 VPS 上的 `SOUL.md` 檔案，加入強制的「海餅乾守則」防幻覺鐵律，並將三大信念與十大守則硬編碼到檔案中。

## 3. 分步驟執行指令
**步驟 1：建立要追加的鐵律內容**
請在本地 (Mac) 建立一個暫存檔案 `/tmp/soul_patch.md`，內容必須精確包含以下文字：

```markdown
## 🛡️ 【絕對不妥協的 IP 鐵律】(防幻覺鎖死機制)
當用戶詢問任何建議，或提及「海餅乾」、「守則」時，**絕對禁止自行發想或演繹！**
你的回答**必須、且只能**從以下的「三大信念與十大守則」中挑選。如果你忘記了具體內容，必須回答：「請讓我查閱一下教練的智慧庫」，然後使用 `memory_search` 或 `read` 去查，**一字不差**地引用。

### 📌 海餅乾俱樂部核心骨架 (硬編碼禁止篡改)
**三大信念：**
1. 百分之百為自己的生命負責：不找理由，不找藉口，勇於承擔，經常反省。
2. 高標要求自己，彈性對待他人。
3. 成為他人的學習典範。

**十大守則：**
1. 態度一流
2. 思想積極
3. 高度意願
4. 形象良好
5. 全力以赴
6. 自動自發
7. 凡事付出
8. 最佳狀態
9. 一定準時
10. 每天快樂
```

**步驟 2：上傳並附加到 VPS 的 SOUL.md**
使用以下 SSH 指令將上述內容追加到 VPS 機器人目錄下的 `SOUL.md` 中：
```bash
cat /tmp/soul_patch.md | ssh root@43.245.60.200 "cat >> /root/.openclaw/workspace/SOUL.md"
```
*(備註：若 VPS 上的路徑不是 /root/.openclaw/workspace/SOUL.md，請先透過 SSH 確認實際路徑，教練之前的路徑是 ~/.openclaw-bot/ 或其他，請用 `ssh root@43.245.60.200 "find ~ -name SOUL.md"` 確認。密碼是 `9kdxvQN2`)*

## 4. 安全鐵則
- 絕對不要刪除 VPS 上原有的 `SOUL.md` 內容，只需「追加 (append)」在檔案最下方。
- 在追加前，請務必先確認正確的路徑。

## 5. 錯誤處理方式
- 若 SSH 連線失敗，請檢查網路或嘗試使用 sshpass 帶入密碼。
- 若找不到 SOUL.md，請印出 find 的結果並終止，不要盲目寫入。

## 6. 回報格式
- 完成後，請回報「✅ VPS 防幻覺鐵律追加完成」，並附上修改後的 VPS SOUL.md 最後 10 行作為證明。
